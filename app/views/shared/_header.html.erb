<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%if !user_signed_in? %>
      <%else%>
        <%= link_to 'Inicio', welcome_index_path, class: "navbar-brand"%>
      <%end%>

    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <%if !user_signed_in? %>
        <%else%>
          <li><%= link_to 'Empleados', employees_path, class: "nav-link"%></li>
          <li><%= link_to 'Evaluaciones', evaluations_path, class: "nav-link"%></li>
          <li><%= link_to 'Gráficas', graphs_new_path, class: "nav-link"%></li>
        <%end%>

      </ul>

      <ul class="nav navbar-nav navbar-right">
        <%if !user_signed_in? %>
          <li><%= link_to 'Iniciar Sesión', new_user_session_path%></li>
        <%else%>
          <li><%= link_to 'Editar usuario', edit_user_registration_path%></li>
          <li><%= link_to 'Cerrar Sesión', destroy_user_session_path, method: :delete%></li>
        <%end%>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
